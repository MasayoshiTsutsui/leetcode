from collections import deque

class Solution:
    def removeDuplicateLetters(self, s: str) -> str:
        last_idx = [-1] * 26

        for i in range():


        stk = deque()
        stk.append(s[0])


        for c in s[1:]:
            if stk[-1] > c:

            